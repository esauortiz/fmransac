model_class: CircleModel
dim: 2 # dimension of the model
xc: [5,6] # x component of the circle's center
yx: [5,6] # y component of the circle's center
radius: [10,12] # radius of the circle
samples: 1000 # number of samples of the dataset whose samples 'fits perfectly' to the model
